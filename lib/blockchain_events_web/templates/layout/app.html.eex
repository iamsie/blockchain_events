<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= render("js_libs.html") %>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <%= unless is_nil(get_flash(@conn, :info)) do %>
    <p class="alert alert-success" role="alert"><%= get_flash(@conn, :info) %></p>
  <% end %>
  <%= unless is_nil(get_flash(@conn, :error)) do %>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
  <% end %>
  <div class="header" style="display: flex; flex-direction: row; justify-content: space-between">
    <h3 style="">blockhain events || online format</h3>
    <h3> Add Event </h3>
  </div>
  <div class="br-bottom"></div>
  <body>
    <div class="wrapper">
      <nav id="sidebar">
        <ul class="list-unstyled components">
          <li>
            <a href="/">
              <i class="fa fa-check"></i>
              <span> Date </span>
            </a>
          </li>
          <li style="margin-top: 10px">
            <a href="/beautifier">
              <i class="fa fa-exchange"></i>
              <span> Event type</span>
            </a>
          </li>
        </ul>
      </nav>
      <!-- Page Content  -->
      <div id="content" style="width: 100%">
        <nav>
          <div class="container-fluid">
            <button type="button" id="sidebarCollapse" class="btn btn-icon btn-outline-secondary">
              <i class="fa fa-align-left"></i>
            </button>
          </div>
        </nav>
        <main role="main" style="margin-left: 2%">
          <%= @inner_content %>
        </main>
      </div>
    </div>
    <%= csrf_meta_tag() %>
  </body>
</html>
