<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= render("js_libs.html") %>
    <%= csrf_meta_tag() %>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <%= unless is_nil(get_flash(@conn, :info)) do %>
    <p class="alert alert-success" role="alert"><%= get_flash(@conn, :info) %></p>
  <% end %>
  <%= unless is_nil(get_flash(@conn, :error)) do %>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
  <% end %>
  <div class="header" style="display: flex; flex-direction: row; justify-content: space-between; align-items: center;">
    <a href="/"> 
      <h2 style="color: #007bff">blockhain events || online format</h2>
       </a>
    <button class="btn btn-sm btn-icon btn-primary" type="button" disabled>
      <span class="btn-inner--icon"><i class="fa fa-plus-circle"></i></span>
      <span class="btn-inner--text">Add Events</span>
    </button>
  </div>
  <div class="br-bottom"></div>
  <body>
    <div style="background-color: white">  <%= @inner_content %> </div>
  </body>
</html>
